/**
 * @fileoverview 同期対象の Notion Database ID 一覧（サンプル）
 *
 * 使い方:
 * 1) このファイルを `DATABASES.js` にコピーする
 * 2) `NOTION_DATABASE_IDS` に同期したい Notion Database ID を配列で設定する
 *
 * 注意:
 * - `DATABASES.js` は `.gitignore` で管理され、リポジトリにはコミットしません
 * - 本プロジェクトでは `NOTION_DATABASE_ID`（スクリプトプロパティ）は使用しません（廃止）
 */
// const NOTION_DATABASE_IDS = [
  // "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
  // "yyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyy",
// ];

// 同期対象を判定する Notion データベースのカラム名
// const TARGET_NOTION_COLUMN_NAME = "公開";
